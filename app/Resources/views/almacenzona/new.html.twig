{{ form_start(form, {'attr':{'action': path('almacenzona_new', {'id': almacen.id}) }}) }}
<div class="row">
   <div class="col-12">
    <div class="card">
      <div class="overlay" style="display:none">
        <i class="fa fa-refresh fa-spin"></i>
      </div>
      <div class="card-body">
        {{ form_widget(form) }}
      </div>
      <div class="card-footer">
        <input type="submit" class="btn btn-sm btn-success d-flex ml-auto" value="Guardar" />
      </div>
    </div>
  </div>
</div>
{{ form_end(form) }}
