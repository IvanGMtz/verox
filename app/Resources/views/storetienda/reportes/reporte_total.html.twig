<!-- Filtro por Asesor y Indicador de Vista -->
<div id="asesorSelector2" class="card shadow mb-4" style="display:none;">
  <div class="card-body">
    <div class="row align-items-center">
      <!-- Indicador de Vista Actual -->
      <div class="col-lg-6 col-md-12 mb-3 mb-lg-0">
        <div id="indicadorVista" class="border-left border-primary pl-3">
          <div class="d-flex align-items-center">
            <i class="fas fa-chart-bar fa-2x text-primary mr-3"></i>
            <div>
              <h4 class="mb-0 font-weight-bold">Reporte General</h4>
              <small class="text-muted">Mostrando datos de todos los asesores</small>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Selector de Asesor -->
      <div class="col-lg-6 col-md-12">
        <label class="font-weight-bold mb-2">
          <i class="fas fa-filter mr-2"></i>Filtrar por Asesor
        </label>
        <select id="asesores2" class="form-control form-control-lg shadow-sm" onchange="cambiarAsesor()">
          <option value="TODOS" selected>📊 Todos los Asesores</option>
          {% for asesor in asesores %}
            <option value="{{ asesor }}">👤 {{ asesor }}</option>
          {% endfor %}
        </select>
      </div>
    </div>
  </div>
</div>

<!-- Cards de Resumen Principal -->
<div id="cards_resumen_principal" class="row mb-4">
</div>

<!-- Cards de KPIs -->
<div id="cards_kpis" class="row mb-4">
</div>

<!-- Cards de Desglose -->
<div id="cards_desglose" class="row mb-4">
</div>

<!-- Detalle de Transacciones -->
<div class="card shadow">
  <div class="card-header bg-dark text-white">
    <h3 class="card-title"><i class="fas fa-list"></i> Detalle de Transacciones</h3>
  </div>
  <div class="card-body p-0">
    <div class="table-responsive">
      <table id="details_total" class="table table-hover table-sm table-bordered mb-0">
        <thead class="thead-light">
          <tr>
            <th class="text-center">Order ID</th>
            <th class="text-center">Fecha</th>
            <th class="text-center">Tipo</th>
            <th class="text-center">Cliente</th>
            <th class="text-center">Estado</th>
            <th class="text-center">Monto</th>
            <th class="text-center">Envío</th>
            <th class="text-center">Total</th>
          </tr>
        </thead>
        <tbody id="detail_data_total">
        </tbody>
      </table>
    </div>
  </div>
</div>

<style>
/* Estilos adicionales para mejorar la apariencia */
#asesorSelector2 {
  border-top: 4px solid #4e73df;
}

#asesores2 {
  border: 2px solid #e3e6f0;
  transition: all 0.3s ease;
}

#asesores2:focus {
  border-color: #4e73df;
  box-shadow: 0 0 0 0.2rem rgba(78, 115, 223, 0.25);
}

#indicadorVista {
  min-height: 60px;
  display: flex;
  align-items: center;
}

/* Responsive ajustes */
@media (max-width: 991px) {
  #indicadorVista {
    border-left: none;
    border-top: 3px solid #4e73df;
    padding-left: 0;
    padding-top: 1rem;
  }
}
</style>