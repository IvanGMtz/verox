<a class="btn btn-default btn-flat btn-sm" href="{{ "{{ path('" ~ route_name_prefix ~ "_index') }}" }}">Regresar</a>
{%- if ('edit' in actions) and (not hide_edit) %}
&nbsp;<a class="btn btn-warning btn-flat btn-sm" href="{{ "{{ path('" ~ route_name_prefix ~ "_edit', { 'id': " ~ entity_singularized ~ "."~ identifier ~" }) }}" }}">Editar</a>
{%- endif %}
{%- if ('delete' in actions) and (not hide_delete) %}
&nbsp;  {{ '{% if is_granted(\'ROLE_SUPER_ADMIN\') %}{{ form_start(delete_form, { \'attr\': {\'style\': \'display:inline-block\'}} ) }}' }}
      <input class="btn btn-danger btn-flat btn-sm" type="submit" value="Eliminar">
  {{ '{{ form_end(delete_form) }}{% endif %}' }}
{%- endif %}

